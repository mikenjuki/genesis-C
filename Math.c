#include <stdio.h>
#include <math.h>

int MathProgram()
{
	int num = 9;
	double A = sqrt(num);
	double B = pow(2, num);
	int C = round(3.145);
	int D = ceil(3.4);
	int E = floor(3.99);
	double F = fabs(-200);
	double G = log(3);
	double H = sin(45);
	double I = cos(45);
	double J = tan(45);

	printf("%lf\n", I);

	return 0;
}